import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'helloFirebase',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
